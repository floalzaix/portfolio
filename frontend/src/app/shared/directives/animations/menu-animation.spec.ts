import { MenuAnimation } from './menu-animation';

describe('MenuAnimation', () => {
  it('should create an instance', () => {
    const directive = new MenuAnimation();
    expect(directive).toBeTruthy();
  });
});
