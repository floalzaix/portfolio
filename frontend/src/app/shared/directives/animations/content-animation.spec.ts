import { ContentAnimation } from './content-animation';

describe('ContentAnimation', () => {
  it('should create an instance', () => {
    const directive = new ContentAnimation();
    expect(directive).toBeTruthy();
  });
});
