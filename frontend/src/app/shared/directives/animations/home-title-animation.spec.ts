import { HomeTitleAnimation } from './home-title-animation';

describe('HomeTitleAnimation', () => {
  it('should create an instance', () => {
    const directive = new HomeTitleAnimation();
    expect(directive).toBeTruthy();
  });
});
