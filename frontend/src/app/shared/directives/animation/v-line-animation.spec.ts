import { VLineAnimation } from './v-line-animation';

describe('VLineAnimation', () => {
  it('should create an instance', () => {
    const directive = new VLineAnimation();
    expect(directive).toBeTruthy();
  });
});
